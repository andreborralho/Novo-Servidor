<%= form_for(@stage) do |f| %>
    <% if @stage.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@stage.errors.count, "error") %> prohibited this show from being saved:</h2>

          <ul>
            <% @stage.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="showField">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>

    <div id="showSubmit" class="actions">
      <%= f.submit %>
    </div>
<% end %>
